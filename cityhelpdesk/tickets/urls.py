from django.conf.urls import url
from tickets import views

urlpatterns = [
    # url(
    #     r'^perfil-autocomplete/$',
    #     views.PerfilAutocomplete.as_view(),
    #     name='perfil-autocomplete',
    # )
]
