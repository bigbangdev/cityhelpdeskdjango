from django.conf.urls import url
from accounts import views as accounts_views

urlpatterns = [
    # url(
    #     r'^perfil-autocomplete/$',
    #     accounts_views.PerfilAutocomplete.as_view(),
    #     name='perfil-autocomplete',
    # )
]
